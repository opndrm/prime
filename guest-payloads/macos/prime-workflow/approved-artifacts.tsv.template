# TEMPLATE ONLY — deliberately invalid until a guest owner replaces every value.
# Do not remove any required component; exactly one tab-separated entry per component.
# Fields: component<TAB>approved-source-id<TAB>owner-approval-id<TAB>artifact-relative-path<TAB>sha256-lowercase<TAB>desired-state
# Artifact paths resolve under $HOME/.config/opndrm-prime/prime-workflow/.
# The source and approval identifiers are owner-reviewed opaque labels, never URLs,
# credentials, or network endpoints. This template is not an authorization record.
wezterm-herdr	REPLACE_SOURCE	REPLACE_APPROVAL	artifacts/wezterm-herdr/REPLACE_FILE	REPLACE_WITH_64_LOWERCASE_HEX	install-plan-only
prime-agent	REPLACE_SOURCE	REPLACE_APPROVAL	artifacts/prime-agent/REPLACE_FILE	REPLACE_WITH_64_LOWERCASE_HEX	install-plan-only
jcode	REPLACE_SOURCE	REPLACE_APPROVAL	artifacts/jcode/REPLACE_FILE	REPLACE_WITH_64_LOWERCASE_HEX	install-plan-only
wayfinder	REPLACE_SOURCE	REPLACE_APPROVAL	artifacts/wayfinder/REPLACE_FILE	REPLACE_WITH_64_LOWERCASE_HEX	install-plan-only
no-mistakes	REPLACE_SOURCE	REPLACE_APPROVAL	artifacts/no-mistakes/REPLACE_FILE	REPLACE_WITH_64_LOWERCASE_HEX	install-inactive-only
